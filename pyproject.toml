[project]
name = "market-quote-assistant"
version = "0.1.0"
description = "A decision-support system for comparing grocery prices across markets."
readme = "README.md"
requires-python = ">=3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]